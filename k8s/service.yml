apiVersion: v1
kind: Service

metadata:
  name: demo-app-service

spec:
  selector:
    app: demo-app

  type: NodePort

  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
